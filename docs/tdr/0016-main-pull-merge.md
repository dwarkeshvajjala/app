# TDR-0016: Preserve local changes when updating main

Date: 2026-09-07.

The requested pull merges origin/main at 11fea12 with local commit a5233a5, preserving both histories. Authentication retains both profile-update repository entry points, session IDs in workspace access tokens, and the incoming session-management services. Daily digests use the incoming repository boundary while retaining the local exclusion of deleted comments. No API contracts were changed by conflict resolution.

Verification and outstanding checks are recorded in docs/implementation/06-delivery.md.
